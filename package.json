{
    "name": "chat_history",
    "description": "A simple way to display a chat history",
    "version": "0.0.1",
    "license": "UNLICENSED",
    "scripts": {
        "build": "npm run clean && npm run copy",
        "clean": "rm -rf dist && mkdir dist",
        "copy": "cpx 'src/**/*.{map,js,json,css,png,html,jpeg}' dist -p",
        "preinstall": "rm -f package-lock.json"
    },
    "dependencies": {
        "lodash": "x.x.x"
    },
    "devDependencies": {
        "less": "x.x.x",
        "typescript": "x.x.x",
        "cpx": "x.x.x",
        "@types/lodash": "x.x.x"
    }
}
